#!/usr/bin/python3
def multiple_returns(sentence):
    length = 0
    first = None

        if sentence:
            length = len(sentence)
            first = sentence[0]
            print(" Lenght: {:d} - First: {}".format(lenght, first))
        else:
            lenght = 0
            first = None
        return
        
